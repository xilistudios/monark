# Project Documentation Rules (Non-Obvious Only)

- **Vault Format**: Detailed specification of the encrypted vault structure is in `docs/vault-format.md`.
- **Cloud Architecture**: Provider-based cloud storage design is documented in `docs/cloud-storage-architecture.md`.
- **Security Patterns**: Crypto implementation details (Argon2, ChaCha20) are in `docs/crypto-architecture.md`.
- **Tauri Commands**: Canonical list of backend commands is in `src-tauri/src/lib.rs` under `invoke_handler`.
